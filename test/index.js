var assert = require("assert");
describe("Check Formats", function () {
  it("install", function () {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
  it("install formatted correctly", function () {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
  it("install formatted correctly", function () {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
  it("install formatted correctly", function () {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});
